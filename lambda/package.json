{
  "name": "lambda-uport-live",
  "version": "0.1.0",
  "description": "The serverless signing functions for uPort Live",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uport": "^0.6.2"
  }
}
